int find_max(int nums[], int len)
{
    int i, max_num = num[0];
    for (i = 1; i < len; i++)
        if (num[i] > max_num)
            // (fill in the missing line here)
    return max_num;
}

// choose one bellow

    nums[i] = max_num
    max_num +=1
    max_num = nums[i]
    max_num += nums[i] 


